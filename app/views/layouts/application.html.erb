<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Cutie Space</title>
    <link href="https://fonts.googleapis.com/css?family=Indie+Flower|Lora|Merriweather|Mukta|Poppins" rel="stylesheet">
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="row">
      <span class="three columns">
        <span class="logo">
          <%=  image_tag("logo.png")  %>
        </span>
      </span>

      <div class="nav-link-box nine columns">

        <% if user_signed_in? %>
          <span class="nav-links">Signed in as <%= current_user.username %></span>
          <span class="nav-links"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></span>
          <span class="nav-links"><%= link_to 'My Page', user_path(current_user) %></span>

        <% else %>
          <span class="hidden nav-links">CutieSpace</span>
          <span class="nav-links"><%= link_to 'Sign In', new_user_session_path %></span>
          <span class="nav-links"><%= link_to 'Sign Up', new_user_registration_path %></span>
        <% end %>

        <span class="nav-links"><%= link_to 'See Everyone', cuties_path %></span>
      </div>
    </nav>

    <span class="alerts">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </span>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
